# ===== ARQUIVO DE EXEMPLO - NÃO CONTÉM VALORES REAIS =====
# Copie este arquivo para .env e preencha com seus valores reais
# NUNCA COMMITE O ARQUIVO .env NO GIT!

# Flask
FLASK_ENV=development
FLASK_DEBUG=1
FLASK_SECRET=seu-secret-key-super-secreto-aqui-mude-isso

# Banco de Dados
DATABASE_URL=sqlite:///meubanco.db

# Railway (Produção)
# DATABASE_URL=postgresql://user:password@host:port/database

# E-mail (opcional - para recuperação de senha)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=seu-email@gmail.com
MAIL_PASSWORD=sua-senha-de-app

# Firebase (se usar)
FIREBASE_CONFIG=path/to/firebase-credentials.json

# Servidor
HOST=0.0.0.0
PORT=5000

# Segurança
# Gere uma chave secreta com: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# APIs Externas (se houver)
API_KEY=
API_SECRET=
